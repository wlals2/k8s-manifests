apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: blog-system
  labels:
    app: web
    tier: frontend
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
