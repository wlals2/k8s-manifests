apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: blog-system
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    # Why: 트리거 정의는 ConfigMap에 있지만, 어떤 Application이 구독할지는 여기서 결정
    notifications.argoproj.io/subscribe.on-sync-failed.discord: ""
    notifications.argoproj.io/subscribe.on-health-degraded.discord: ""
    notifications.argoproj.io/subscribe.on-sync-succeeded.discord: ""
spec:
  project: default
  source:
    repoURL: https://github.com/wlals2/k8s-manifests.git
    targetRevision: main
    path: services/blog-system
  destination:
    server: https://kubernetes.default.svc
    namespace: blog-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true

  # Argo Rollouts가 자동으로 관리하는 필드 무시
  ignoreDifferences:
  - group: networking.istio.io
    kind: DestinationRule
    name: web-dest-rule
    jsonPointers:
    - /spec/subsets/0/labels
    - /spec/subsets/1/labels
  - group: networking.istio.io
    kind: DestinationRule
    name: was-dest-rule
    jsonPointers:
    - /spec/subsets/0/labels
    - /spec/subsets/1/labels
