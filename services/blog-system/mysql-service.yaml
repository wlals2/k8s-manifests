apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: blog-system
  labels:
    app: mysql
spec:
  type: ClusterIP
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
    name: mysql
