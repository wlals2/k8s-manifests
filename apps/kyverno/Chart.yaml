apiVersion: v2
name: my-kyverno
description: Wrapper chart for Kyverno Policy Engine
type: application
version: 0.1.0
appVersion: "v1.17.1"

# Why: Kyverno를 직접 참조하지 않고 Wrapper로 감싸는 이유
#      values.yaml을 Git에서 관리 → ArgoCD가 drift 감지 가능
dependencies:
  - name: kyverno
    version: 3.7.1
    repository: https://kyverno.github.io/kyverno/
